# VS Code from terminal  ( ‘code' ) after adding it to the path: Launch VS Code.

Open the Command Palette (Cmd+Shift+P) and type:

'shell command'
- to find the Shell Command: Install 'code' command in PATH command.
