# \du = roles
# \dn = list-of-schemas
# \l = list databases.
# \c db_name = to connect to a certain database
# \dt = list tables

# grant all privileges on database dev to xxx with login;

# createdb name
# psql -d name -U urname

# create role rrr;
# alter user uuu with SUPERUSER;
# alter user uuu with login;
# alter user uuu with createdb;
