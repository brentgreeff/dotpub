# nginx -V
# alias check_conf="/usr/sbin/nginx -t"
# alias full_conf="/usr/sbin/nginx -T"

# /usr/local/etc/nginx/nginx.conf

# sudo nginx -s reload

# sudo pkill nginx

# brew services list
# brew services run nginx
# -- Run the service formula without registering to launch on boot.
